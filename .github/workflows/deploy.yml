name: Deploy to Deno Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write   # åè®¸æ¿å° OIDC ä»¤ç
      contents: read    # åè®¸æ£åºä»åº

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Upload to Deno Deploy
        uses: denoland/deployctl@v1
        with:
          project: picstudio # æ¿æ¢ä¸ºä½ ç Deno Deploy é¡¹ç®å
          entrypoint: serve.ts
          root: .